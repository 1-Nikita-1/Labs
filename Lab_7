const EventEmitter = require('events');

class MessageBus extends EventEmitter {}
const bus = new MessageBus();

class Sender {
    send(message) {
        console.log(`\n[Sender] Sending: ${message}`);
        bus.emit('message', message);
    }
}

class Logger {
    constructor() {
        this.listener = (msg) => {
            console.log(`[Logger] Received: ${msg}`);
        };

        bus.on('message', this.listener);
    }

    unsubscribe() {
        bus.off('message', this.listener);
        console.log('[Logger] Unsubscribed');
    }
}

class Analyzer {
    constructor() {
        this.listener = (msg) => {
            console.log(`[Analyzer] Message length: ${msg.length}`);
        };

        bus.on('message', this.listener);
    }

    unsubscribe() {
        bus.off('message', this.listener);
        console.log('[Analyzer] Unsubscribed');
    }
}

class AutoResponder {
    constructor() {
        this.listener = (msg) => {
            if (msg.includes('hello')) {
                console.log('[AutoResponder] Reply: Hi there!');
            }
        };

        bus.on('message', this.listener);
    }

    unsubscribe() {
        bus.off('message', this.listener);
        console.log('[AutoResponder] Unsubscribed');
    }
}

const sender = new Sender();

const logger = new Logger();
const analyzer = new Analyzer();
const responder = new AutoResponder();

sender.send('hello world');
sender.send('reactive systems are cool');

logger.unsubscribe();

sender.send('this message is after unsubscribe');

analyzer.unsubscribe();
responder.unsubscribe();

sender.send('no listeners now');